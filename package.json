{
  "name": "ironpermjs",
  "version": "0.0.0",
  "private": true,
  "description": "Authorization integration and analysis tooling built around CASL",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "pnpm --filter @ironpermjs/core build && pnpm --filter @ironpermjs/server build && pnpm --filter @ironpermjs/react build && pnpm --filter @ironpermjs/next build && pnpm --filter @ironpermjs/analyzer build && pnpm --filter @ironpermjs/graph build && pnpm --filter @ironpermjs/diff build && pnpm --filter @ironpermjs/reporter build && pnpm --filter @ironpermjs/cli build",
    "check": "pnpm format:check && pnpm build && pnpm lint && pnpm typecheck && pnpm test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "ironpermjs": "node packages/cli/dist/bin.js",
    "release:dry-run": "pnpm check && node scripts/release-packages.mjs dry-run",
    "release:pack": "pnpm build && node scripts/release-packages.mjs pack",
    "release:publish": "pnpm check && node scripts/release-packages.mjs publish",
    "release:smoke": "pnpm release:verify && node scripts/smoke-packed-packages.mjs",
    "release:verify": "pnpm build && node scripts/release-packages.mjs verify",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit && tsc -p examples/next-basic/tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "eslint": "^10.8.1",
    "jsdom": "26.1.0",
    "prettier": "^3.6.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.41.0",
    "vitest": "^4.1.11"
  },
  "pnpm": {
    "overrides": {
      "typescript": "5.9.3"
    }
  }
}
